TELEGRAM_BOT_TOKEN=
ADMIN_TELEGRAM_IDS=

# Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIALS_ENCRYPTION_KEY=

# Публичный URL сервиса (для eBay Marketplace Account Deletion)
# Для Docker на VPS: http://<VPS_IP>:18080 (или HTTPS-домен за reverse proxy)
PUBLIC_BASE_URL=https://example.com

POLL_INTERVAL_SEC=300
SEEN_ITEMS_TTL_DAYS=90
DATABASE_PATH=data/bot.db
LOG_LEVEL=INFO

# Порт FastAPI внутри контейнера (снаружи в compose проброшен 18080)
WEB_HOST=0.0.0.0
WEB_PORT=8080

# Опционально
# HTTP_PROXY=
